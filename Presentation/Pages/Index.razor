@page "/"
@using Fluxor
@using Presentation.Store
@inject IDispatcher _dispatcher

<Importer />
<Reconciler />

@code
{
    protected override Task OnInitializedAsync()
    {
        _dispatcher.Dispatch(new TransactionsLoadAction(String.Empty));
        return base.OnInitializedAsync();
    }
}
